name: myservice
sid: myservice
description: "Description of my service"
tasks:
  taskX:
    inputs:
      foo:
        type: String
      bar:
        type: String
    outputs:
      success:
        data:
          x:
            type: String
      error:
        data:
          message:
            type: String
events:
  started:
    data:
      x:
        type: Boolean
