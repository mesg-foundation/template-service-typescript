name: myservice
sid: myservice
description: "Description of my service"
tasks:
  taskX:
    inputs:
      foo:
        type: String
      bar:
        type: String
    outputs:
      message:
        type: String
events:
  started:
    data:
      x:
        type: Boolean
